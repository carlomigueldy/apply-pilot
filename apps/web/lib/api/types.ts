/**
 * Shared API types.
 *
 * Re-exported from the workspace package `@applypilot/shared-types`, which is
 * generated/maintained by a separate workspace agent. Until that package is
 * linked, this module will not resolve — that is expected during early setup.
 */
export * from "@applypilot/shared-types";
