"""Agent layer: LangGraph graphs, nodes, prompts, LLM providers, and shared state.

Everything in this package is synchronous (``graph.invoke``, sync nodes, sync
providers) per the ApplyPilot backend convention.
"""
